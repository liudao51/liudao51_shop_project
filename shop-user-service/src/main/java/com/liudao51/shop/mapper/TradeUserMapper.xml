<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liudao51.shop.mapper.TradeUserMapper">

    <select id="selectById" parameterType="map" resultType="com.liudao51.shop.entity.po.TradeUser">
        SELECT
            a.user_id,
            a.user_name,
            a.user_password,
            a.user_mobile,
            a.user_score,
            a.user_reg_time,
            a.user_money
        FROM trade_user AS a
        WHERE 1=1
        <choose>
            <when test="parameter.user_id != null and parameter.user_id != ''">
                AND a.user_id=#{parameter.user_id}
            </when>
            <otherwise>
                AND a.user_id='-1'
            </otherwise>
        </choose>
        LIMIT 0,1
    </select>

    <select id="selectOne" parameterType="map" resultType="com.liudao51.shop.entity.po.TradeUser">
        SELECT
            a.user_id,
            a.user_name,
            a.user_password,
            a.user_mobile,
            a.user_score,
            a.user_reg_time,
            a.user_money
        FROM trade_user AS a
        WHERE 1=1
        <if test="parameter.user_id!=null and parameter.user_id!=''">
            AND a.user_id=#{parameter.user_id}
        </if>
        LIMIT 0,1
    </select>

    <select id="selectList" parameterType="map" resultType="com.liudao51.shop.entity.po.TradeUser">
        SELECT
            a.user_id,
            a.user_name,
            a.user_password,
            a.user_mobile,
            a.user_score,
            a.user_reg_time,
            a.user_money
        FROM trade_user AS a
    </select>
</mapper>
